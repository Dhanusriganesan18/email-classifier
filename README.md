# email-classifier
classifier that uses machine learning to identify whether a given text message is spam or not spam. It is trained using the SMS Spam Collection dataset and compares multiple models such as Naive Bayes, Logistic Regression, and Random Forest.
